server:
  port: 8082

spring:
  application:
    name: ems-mc

  datasource:
    url: jdbc:postgresql://mc_db:5432/mc
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
